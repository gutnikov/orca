Pick an evaluation to run

Demo invocation of the `run-evaluations` workflow. This single-state flow
scans `.orca/evals/`, presents the directories via the web form UI, and
records which one the user picked. The flow does not yet run the chosen
evaluation — selection is the entire scope of step 1.
